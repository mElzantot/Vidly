import React from "react";
// import querystring from "query-string";

const Customers = (props) => {
  // const result = querystring.parse(props.location.search);
  return <h4 className="m-4">Customers </h4>;
};

export default Customers;
